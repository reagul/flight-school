platform: linux

inputs:
- name: flight-school

run:
  path: ./flight-school/ci/test.sh

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: 2.3.0
resources:
- name: flight-school
  type: git
  source:
    uri: https://github.com/reagul/flight-school
    branch: master

jobs:
- name: test-app
  plan:
  - get: flight-school
    trigger: true
  - task: tests
    file: flight-school/build.yml
